# Frontend Environment Variables

# API endpoint base URL (mặc định http://localhost:8000)
REACT_APP_API_BASE_URL=http://localhost:8000

# Giới hạn MB hiển thị + validate phía client — nên trùng MAX_UPLOAD_MB trên backend
REACT_APP_MAX_UPLOAD_MB=50
